#include "pebble.h"
#include "event.h"
#include "map.h"
#include "player.h"
#include "combat.h"
// AUTO-GENERATED by gen_levels.py — Legend of Pebble level 3.

static const DialogPage s_m3_note0_pages[] = {
    { "THE RIFT\nRUNS\nDEEPER." },
    { "STAY\nSHARP." },
};
static const DialogDef s_m3_note0_dialog = {
    .title = "A NOTE",
    .pages = s_m3_note0_pages,
    .page_count = ARRAY_LENGTH(s_m3_note0_pages),
};

static const EventCmd s_m3_ev0_cmds[] = {
    { .type=CMD_DISPLAY_MESSAGE, .message={ "LEVEL 3", 25 } },
};
static const EventCmd s_m3_ev1_cmds[] = {
    { .type=CMD_DISPLAY_MESSAGE, .message={ "DESCENDING...", 20 } },
    { .type=CMD_CHANGE_MAP, .change_map={ 3, 1, 1, 2 } },
};
static const EventCmd s_m3_ev2_cmds[] = {
    { .type=CMD_FLAG_CHECK, .flag_check={ 5, -1 } },
    { .type=CMD_FLAG_CHANGE, .flag_change={ 5, true } },
    { .type=CMD_SET_SPELLBOOK, .set_spellbook={ 1 } },
    { .type=CMD_DISPLAY_ICON, .display_icon={ 12, 25 } },
    { .type=CMD_DISPLAY_MESSAGE, .message={ "SPELLBOOK:\nHEAL", 30 } },
    { .type=CMD_CHANGE_DECOR, .change_decor={ 15, 3, DECOR_NONE } },
};
static const EventCmd s_m3_ev3_cmds[] = {
    { .type=CMD_DIALOG, .dialog={ &s_m3_note0_dialog } },
};

const Event MAP3_EVENT_TABLE[] = {
    { s_m3_ev0_cmds, ARRAY_LENGTH(s_m3_ev0_cmds), NO_EVENT_TRIGGER },
    { s_m3_ev1_cmds, ARRAY_LENGTH(s_m3_ev1_cmds), EVENT_TRIGGER_STEP },
    { s_m3_ev2_cmds, ARRAY_LENGTH(s_m3_ev2_cmds), EVENT_TRIGGER_STEP },
    { s_m3_ev3_cmds, ARRAY_LENGTH(s_m3_ev3_cmds), EVENT_TRIGGER_FORWARD },
};
#define MAP3_EVENT_COUNT ARRAY_LENGTH(MAP3_EVENT_TABLE)
#define MAP3_START_EVENT 0
