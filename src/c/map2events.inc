#include "pebble.h"
#include "event.h"
#include "map.h"
#include "player.h"
#include "combat.h"
// AUTO-GENERATED by gen_levels.py — Legend of Pebble level 2.

static const DialogPage s_m2_intro_pages[] = {
    { "THE AIR\nHUMS WITH\nSTATIC." },
    { "OTHER\nWATCHES\nWERE PULLED\nHERE TOO." },
    { "SOMEONE\nLEFT A\nNOTE..." },
};
static const DialogDef s_m2_intro_dialog = {
    .title = "THE RIFT",
    .pages = s_m2_intro_pages,
    .page_count = ARRAY_LENGTH(s_m2_intro_pages),
};

static const DialogPage s_m2_note0_pages[] = {
    { "I'M MAREN." },
    { "I WAS A\nCODER.\nLIKE YOU." },
    { "THE SIGNAL\nKEEPERS\nRULE THE\nRIFT." },
    { "FIND THE\nCORE.\nBREAK THE\nSIGNAL." },
};
static const DialogDef s_m2_note0_dialog = {
    .title = "A NOTE",
    .pages = s_m2_note0_pages,
    .page_count = ARRAY_LENGTH(s_m2_note0_pages),
};

static const EventCmd s_m2_ev0_cmds[] = {
    { .type=CMD_DIALOG, .dialog={ &s_m2_intro_dialog } },
};
static const EventCmd s_m2_ev1_cmds[] = {
    { .type=CMD_DISPLAY_MESSAGE, .message={ "DESCENDING...", 20 } },
    { .type=CMD_CHANGE_MAP, .change_map={ 2, 8, 2, 1 } },
};
static const EventCmd s_m2_ev2_cmds[] = {
    { .type=CMD_DISPLAY_MESSAGE, .message={ "BLINK!", 15 } },
    { .type=CMD_MOVE_PLAYER, .move_player={ 14, 3, 1 } },
};
static const EventCmd s_m2_ev3_cmds[] = {
    { .type=CMD_FLAG_CHECK, .flag_check={ 3, -1 } },
    { .type=CMD_FLAG_CHANGE, .flag_change={ 3, true } },
    { .type=CMD_GIVE_ITEM, .give_item={ 1, 1 } },
    { .type=CMD_DISPLAY_ICON, .display_icon={ 11, 25 } },
    { .type=CMD_DISPLAY_MESSAGE, .message={ "FOUND A KEY", 25 } },
    { .type=CMD_CHANGE_DECOR, .change_decor={ 2, 8, DECOR_NONE } },
};
static const EventCmd s_m2_ev4_cmds[] = {
    { .type=CMD_DISPLAY_MESSAGE, .message={ "NEEDS A KEY", 20 } },
};
static const EventCmd s_m2_ev5_cmds[] = {
    { .type=CMD_FLAG_CHECK, .flag_check={ 4, -1 } },
    { .type=CMD_ITEM_CHECK, .item_check={ 1, 4 } },
    { .type=CMD_TAKE_ITEM, .take_item={ 1, 1 } },
    { .type=CMD_FLAG_CHANGE, .flag_change={ 4, true } },
    { .type=CMD_CHANGE_DECOR, .change_decor={ 13, 11, DECOR_ALCOVE_FULL } },
    { .type=CMD_CHANGE_DECOR, .change_decor={ 16, 11, DECOR_NONE } },
    { .type=CMD_DISPLAY_MESSAGE, .message={ "THE GATE OPENS", 25 } },
};
static const EventCmd s_m2_ev6_cmds[] = {
    { .type=CMD_DIALOG, .dialog={ &s_m2_note0_dialog } },
};
static const EventCmd s_m2_ev7_cmds[] = {
    { .type=CMD_OPEN_SHOP },
};

const Event MAP2_EVENT_TABLE[] = {
    { s_m2_ev0_cmds, ARRAY_LENGTH(s_m2_ev0_cmds), NO_EVENT_TRIGGER },
    { s_m2_ev1_cmds, ARRAY_LENGTH(s_m2_ev1_cmds), EVENT_TRIGGER_STEP },
    { s_m2_ev2_cmds, ARRAY_LENGTH(s_m2_ev2_cmds), EVENT_TRIGGER_STEP },
    { s_m2_ev3_cmds, ARRAY_LENGTH(s_m2_ev3_cmds), EVENT_TRIGGER_STEP },
    { s_m2_ev4_cmds, ARRAY_LENGTH(s_m2_ev4_cmds), NO_EVENT_TRIGGER },
    { s_m2_ev5_cmds, ARRAY_LENGTH(s_m2_ev5_cmds), EVENT_TRIGGER_FORWARD },
    { s_m2_ev6_cmds, ARRAY_LENGTH(s_m2_ev6_cmds), EVENT_TRIGGER_FORWARD },
    { s_m2_ev7_cmds, ARRAY_LENGTH(s_m2_ev7_cmds), EVENT_TRIGGER_FORWARD },
};
#define MAP2_EVENT_COUNT ARRAY_LENGTH(MAP2_EVENT_TABLE)
#define MAP2_START_EVENT 0
